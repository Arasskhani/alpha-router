# Development / CI tooling. Runtime dependencies live in requirements.txt.
# Pinned exactly so a lint or type-check result is reproducible between a
# developer machine and the pipeline.
pytest==9.1.1
pytest-asyncio==1.4.0
ruff==0.16.7
mypy==1.20.2
mypy-baseline==0.7.4
vulture==2.16
