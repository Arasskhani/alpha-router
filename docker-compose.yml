services:
  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_USER: ${POSTGRES_USER:-alpha-router}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:?POSTGRES_PASSWORD must be set in .env}
      POSTGRES_DB: ${POSTGRES_DB:-alpha-router}
    command:
      - postgres
      - -c
      - max_connections=200
      - -c
      - shared_buffers=512MB
      - -c
      - effective_cache_size=1536MB
      - -c
      - work_mem=8MB
    volumes:
      - alpha_router_pg:/var/lib/postgresql/data
    ports:
      - "127.0.0.1:5432:5432"
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U alpha-router"]
      interval: 5s
      timeout: 5s
      retries: 5

  pgbouncer:
    image: edoburu/pgbouncer:1.22.1-p0
    environment:
      DATABASE_URL: postgres://${POSTGRES_USER:-alpha-router}:${POSTGRES_PASSWORD}@postgres:5432/${POSTGRES_DB:-alpha-router}
      # edoburu image defaults to 5432; set 6432 so app/host use standard PgBouncer port
      LISTEN_PORT: 6432
      # Postgres 16 uses SCRAM; default pgbouncer auth is MD5
      AUTH_TYPE: scram-sha-256
      POOL_MODE: transaction
      MAX_CLIENT_CONN: 4000
      DEFAULT_POOL_SIZE: 90
      MIN_POOL_SIZE: 20
      RESERVE_POOL_SIZE: 15
      SERVER_RESET_QUERY: DISCARD ALL
    ports:
      - "127.0.0.1:6432:6432"
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -h 127.0.0.1 -p 6432 -U alpha-router -d alpha-router"]
      interval: 5s
      timeout: 5s
      retries: 5
      start_period: 5s
    depends_on:
      postgres:
        condition: service_healthy

  redis:
    image: redis:7-alpine
    # Require authentication when REDIS_PASSWORD is set; empty (default) keeps
    # legacy no-auth behavior for existing single-box deployments. When set,
    # wire REDIS_URL=redis://:${REDIS_PASSWORD}@redis:6379/0 in the alpha-router service.
    command: ["sh", "-c", "exec redis-server --requirepass \"$${REDIS_PASSWORD:-}\""]
    environment:
      REDIS_PASSWORD: ${REDIS_PASSWORD:-}
    ports:
      - "127.0.0.1:6379:6379"
    volumes:
      - alpha_router_redis:/data

  minio:
    image: minio/minio:RELEASE.2025-04-22T22-12-26Z
    command: server /data --console-address ":9001"
    environment:
      MINIO_ROOT_USER: ${S3_ACCESS_KEY:?S3_ACCESS_KEY must be set in .env}
      MINIO_ROOT_PASSWORD: ${S3_SECRET_KEY:?S3_SECRET_KEY must be set in .env}
    ports:
      - "127.0.0.1:9000:9000"
      - "127.0.0.1:9001:9001"
    volumes:
      - alpha_router_minio:/data

  # Code-interpreter sandbox image. Not started as a long-running service; it is
  # spawned on demand as a disposable, network-less container by the alpha-router app.
  # Build explicitly with: docker compose build sandbox
  sandbox:
    build:
      context: ./sandbox
      dockerfile: Dockerfile
    image: alpha-router-sandbox:latest
    profiles: ["build-only"]
    restart: "no"

  alpha_router:
    build:
      context: .
      dockerfile: Dockerfile
    env_file:
      - .env
    ports:
      - "8080:8080"
    environment:
      # Docker network overrides (env_file supplies pool/worker/rate-limit tuning)
      LDAP_BRIDGE_URL: http://host.docker.internal:8765
      LDAP_BRIDGE_TOKEN: alpha-router-ldap-bridge
      # Code interpreter spawns this image via the mounted Docker socket.
      CODE_SANDBOX_IMAGE: alpha-router-sandbox:latest
    volumes:
      # Required so the app can spawn disposable sandbox containers (Phase 2).
      - /var/run/docker.sock:/var/run/docker.sock
    command:
      - uvicorn
      - app.main:app
      - --host
      - 0.0.0.0
      - --port
      - "8080"
      - --workers
      - "${UVICORN_WORKERS:-4}"
    depends_on:
      pgbouncer:
        condition: service_healthy
      redis:
        condition: service_started
      minio:
        condition: service_started

volumes:
  alpha_router_pg:
  alpha_router_redis:
  alpha_router_minio:
